import Results from './ResultHistory.jsx'

export default function ResultHistoryPage({ changeResultPublication, results }) {
  return <Results changeResultPublication={changeResultPublication} results={results} />
}
